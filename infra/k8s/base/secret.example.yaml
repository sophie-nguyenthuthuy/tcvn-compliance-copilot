# EXAMPLE ONLY — do NOT commit a real secret. In production this should be
# managed by ExternalSecrets / SealedSecrets / Vault, not raw YAML.
apiVersion: v1
kind: Secret
metadata:
  name: tcvn-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql+asyncpg://USER:PASS@postgres:5432/tcvn_copilot"
  REDIS_URL: "redis://redis:6379/0"
  CELERY_BROKER_URL: "redis://redis:6379/1"
  CELERY_RESULT_BACKEND: "redis://redis:6379/2"
  S3_ACCESS_KEY: "REPLACE_ME"
  S3_SECRET_KEY: "REPLACE_ME"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  API_SECRET_KEY: "REPLACE_ME"
